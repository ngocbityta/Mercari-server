name: Build and test NestJS project
on:
  push:
    branches:
      - main
  workflow_dispatch:
    
jobs:
  test:
    runs-on: ubuntu-24.04
    permissions:
      contents: read #This is required for actions/checkout
    steps:
    - name: Checkout
      uses: actions/checkout@v6.0.2
    
    - name: Setup Node.js environment
      uses: actions/setup-node@v6.3.0
      with:
        # Version Spec of the version to use. Examples: 12.x, 10.15.1, >=10.15.0.
        node-version: 24.14.0 # Latest LTS (for now)

    - name: npm install, build and test
      env:
        DATABASE_URL: dummy_database_url_for_prisma_generate
        # Check https://github.com/prisma/prisma/issues/28590#issuecomment-3599662504 for more details
      run: |
        npm ci
        npx prisma generate
        npx nest build 
        npx jest
        
  image-build:
    needs: test
    runs-on: ubuntu-24.04
    permissions:
      contents: read #This is required for actions/checkout
      packages: write #This is required for pushing to GHCR
    steps:
    - name: Checkout
      uses: actions/checkout@v6.0.2

    - name: Login to GHCR
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build image
      env:
        PRISMA_DATABASE_URL: dummy_database_url_for_prisma_generate
        DOCKER_BUILDKIT: 1
      run: |
        docker build --secret id=DATABASE_URL,env=PRISMA_DATABASE_URL -t ghcr.io/ngocbityta/mercari-server:${{ github.sha }} .

    - name: Push image
      run: |
        docker push ghcr.io/ngocbityta/mercari-server:${{ github.sha }}
        
  deploy:
    needs: [test, image-build]
    runs-on: ubuntu-24.04
    permissions:
      contents: read #This is required for actions/checkout
    steps:
    - name: Checkout
      uses: actions/checkout@v6.0.2
      
    - name: Scp compose file
      uses: appleboy/scp-action@v1.0.0
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.ACTION_DEPLOY_USERNAME }}
        key: ${{ secrets.ACTION_DEPLOY_SSH_KEY }}
        source: "compose.yml"
        target: "/home/deploy/group1"

    - name: Deploy
      uses: appleboy/ssh-action@v1.2.5
      env:
        DATABASE_URL: ${{ secrets.PRISMA_DATABASE_URL }}
        IMAGE_TAG: ${{ github.sha }}
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.ACTION_DEPLOY_USERNAME }}
        key: ${{ secrets.ACTION_DEPLOY_SSH_KEY }}
        envs: DATABASE_URL, IMAGE_TAG
        script: |
          cd /home/deploy/group1
          docker compose pull
          docker compose up -d --force-recreate
